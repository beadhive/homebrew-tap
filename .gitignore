# Local Homebrew bottle build artifacts (just bottle) — uploaded to GitHub
# Releases, not committed here.
*.bottle.tar.gz
*.bottle.json
*.bottle.*.tar.gz

# beads issue-tracker local state, if this repo ever gets onboarded as a rig.
.beads/
